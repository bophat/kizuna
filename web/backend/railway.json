{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "healthcheckPath": "/api/shop/exchange-rates/",
    "restartPolicyType": "ON_FAILURE"
  }
}
